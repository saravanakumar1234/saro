#include<stdio.h>
int  main()
{
int n,sum=0,i=1;
printf("enter the value of n");
scanf("%d",&n);
while(i<=n)
{
if(i%2==1)
}
printf("the odd numbers between two intervals %d is:%d",n ,sum);
retuen 0;
}
